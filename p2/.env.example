# Copy to .env and adjust. Plain KEY=value — NOT Ruby.
# No quotes needed, no #{} interpolation. The Vagrantfile reads these
# and builds derived values (VM names) itself.

LOGIN=ijaija

SERVER_IP=192.168.56.110

VMS_MAC_ADDR=52:00:00:00:00:01

SERVER_SSH_PORT=50122
LAN_PORT=12345
